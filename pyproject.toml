[project]
name = "devops-agent"
version = "0.1.0"
description = "DevOps Supervisor Agent with A2A Protocol"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "strands-agents[a2a]>=1.18.0",  # A2A support needed for AWS agent A2A server
    "strands-agents-tools",
    "streamlit",
    "boto3",
    "python-dotenv",
    "mcp",
    "httpx",
    "pydantic-settings",
    "fastapi>=0.104.0",
    "uvicorn[standard]>=0.24.0",
    "a2a-sdk",  # A2A client SDK needed for supervisor to communicate with AWS agent
]

[tool.uv]
dev-dependencies = [
    "pytest",
    "black",
    "isort",
]
